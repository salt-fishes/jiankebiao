# 解析规则包方案评估与后续路线

> 状态：评估结论 + 更新计划（基于 v1 落地现状与 `RULEPACK_V2_PLAN.md`）
> 关联代码：`ComposeApp/app/src/main/java/com/saltfish/simple/schedule/`、`rulepack-dev/`

## 0. 结论摘要

**方案可行，且是当前约束集下的正确解**；v1 的核心风险（解释器启发式漂移）已被正确诊断，
v2（规则执行器 + 判据数据化）是必要的下一步。适应性在「三类已支持布局族」内为高，
受不联网红线约束，普通用户的开箱适应性取决于 AI 工作台的效率而非应用本身。
鲁棒性整体良好（纯数据 + 导入期校验 + 人在环节确认），存在两个须在 v2 一并修复的
工程缺口（ReDoS 探测、导入大小上限）和一个产品级缺口（兜底字段无来源标注）。

## 1. 可行性论证

### 1.1 论域封闭性（可行性的根基）

「教务导出文本 → 课程字段」的差异空间是**封闭小集合**，不是开放问题：

- 语义字段有限且全行业同构：课名/类型/教师/教室/校区/周次/节次/单双周/教学班/学分。
  这是教务系统的业务本质决定的，不随学校变化（口诀「换个学校它还成立吗」的根据）。
- 文本形态经实测收敛为三类布局族：键值链式（正方）、行前缀式（强智 Web/截图卡）、
  位置式（强智 PDF）。一轮适配三个真实样本无一溢出此分类。
- 判据全部正则可表达：v1 适配新增的 5 处解释器启发式（行尾数字防误判、职称括注、
  【周次】同行、教室行形状、教学班编号形状）逐一检验，均为单行上的正则判据，
  无一处需要跨行关系或条件逻辑。

结论：把「判据」从「机制」中参数化出来，在该论域内表达力是充分的。

### 1.2 纯数据 DSL 的充分性

v2 设计（有序规则列表 + 捕获组声明 + 白名单变换 + 先到先得）本质是
「模式→动作」表驱动的经典形态（UA 样式表、邮件过滤 DSL 同构）。关键性质：

- 变换白名单（stripTrailingParen / splitCampus / cleanTeacher / excludeChars）的
  语义**已经以解释器代码形式存在**，v2 只是把「是否启用、作用于哪个字段」从
  代码硬编码改为包数据——是参数化，不是新增语义，实现风险低；
- 有序先到先得语义平凡、可预测，Python 模拟器可精确镜像（除正则方言差异外）；
- v1 语法糖编译期展开（前缀→规则、keyMap→边界锚定正则）保证旧包零迁移。

### 1.3 明确的不可行边界（必须写成承诺而非含糊）

以下情况规则包**无法**表达，遇到即显式失败，不做优雅降级伪装成功：

1. 跨行/跨字段关系判据（"教室行的下一行是教师"这类）——当前 DSL 无相对位置原语；
2. 无任何结构特征的自由粘连文本；
3. 列表式布局（一行一课无网格）——需要第三种分割机制，与判据层无关，保持后置；
4. Excel 班级课表——独立解析器（`ScheduleXlsParser`），不在规则包管控内（见 §6 P4）。

### 1.4 与备选方案的对比（为什么不是别的）

- **脚本引擎（Lua/JS）**：表达力上限更高，但违反纯数据红线，且把「适配」变成
  「写代码」，校验、沙箱、模拟器的成本全部失控。否决理由充分。
- **联网 LLM 解析**：违反不联网红线，且结果不可复现、无法本地调试。否决。
- **纯内置启发式持续堆叠（v1 现状）**：每校一发版，用户端永不收敛。这正是
  v1 实测暴露的漂移路径，v2 是对它的直接修正而非另一个并行方案。

## 2. 适应性分析

| 场景 | v2 后适配成本 | 通道 |
|---|---|---|
| 同系统换学校（正方/强智变体） | 词表 + 指纹微调，分钟级 | 往往无需新包（指纹与 key 命中即可） |
| 已知布局族的新系统 | 一个 JSON 包，AI 工作台小时级闭环 | 零发版 |
| OCR 变体漂移 | 加 markAliases/分隔符 | 零发版 |
| 新布局族（list 等） | 需改解释器 → 发版 | 显式不支持，见触发条件 §1.3 |

**关键限制——分发面**：不联网红线排除了规则包仓库/CDN，包的传播只有两条路：
内置包（随发版）与文件导入（AI 工作台产出 / 用户间文件分享，JSON 仅数 KB，
微信传文件即可）。因此「零发版适配」的真实含义是：**愿意跑一遍 AI 工作台流程的
用户或被远程协助的用户**获得即时适配；普通用户的开箱体验取决于内置包覆盖率。
这个限制是红线内生成本，不可消除，只能通过降低单包制作成本（模拟器、工作台）
和鼓励包文件流转来摊薄。方案评估为可行正是基于「单包成本可压到小时级」。

## 3. 鲁棒性分析

### 3.1 现有防线（v1 已验证，v2 全部保留）

| 防线 | 位置 | 效果 |
|---|---|---|
| 导入期全量校验（结构/上限/正则编译） | `ParseRulePack.validateAndCompile` | 不合法包绝不落盘 |
| 纯数据红线（无可执行逻辑） | schema + 白名单 | 无代码执行面 |
| 被匹配文本限长 4000 | 解释器侧 | 限制回溯输入规模 |
| 指纹 minHits + 同分导入包优先 | `RulePackStore.pickFor` | 防误选；用户显式选包兜底 |
| 通用兜底包 + PDF 退正方包 | 同上 | 全不中时有确定性降级 |
| 人在环节：导入确认弹窗逐条核对 | UI | 最终防线，兜底错配可被人工拦截 |
| 30 单测 + 三样本端到端 | 测试 | 回归基线 |

### 3.2 残余风险（按严重度排序，v2 必须处理的标 ⚠）

**R1 ⚠ ReDoS 无运行时防线**。Java 正则无超时机制；`validate.py` 的嵌套量词
粗筛只是提示不是拒绝。一条 200 字符内的恶意/无知正则（如 `(.+)+X` 形态）作用于
4000 字符文本仍可数分钟级卡死解析线程。**对策（导入期探测）**：导入时对每条
正则跑对抗样本（定长重复串 + 不匹配哨兵），在看门狗线程内限时 ~500ms，
超时即拒收。这把防线从「作者自觉」移到「导入强制」，成本一次、永久生效。

**R2 ⚠ 导入无大小上限**。`AppShell.kt:214` 直接 `readText()`，百 MB JSON 可致
OOM/ANR。**对策**：导入入口限制 ≤ 256KB 并给出明确报错文案；`RulePackStore`
加载时同样跳过超限文件。

**R3 兜底字段静默错配**。位置式兜底（positionalTeacher/positionalRoom 及行形状
规则）可能产出「貌似合理」的错误教师/教室。当前设计靠确认弹窗人工拦截是成立的，
但用户不知道该重点看什么。**对策**：规则执行器输出命中轨迹后，在调试日志与
（轻量地）确认界面上标注「该字段由兜底规则填入」，引导核对注意力。

**R4 模拟器语义漂移**。Python re ≠ Java regex（命名组、 possessive 量词等方言）。
对策即现有文档定位：模拟器只做提示，真值以应用导入为准；黄金样本库（§5 M-A3）
提供双向对照以尽早暴露漂移。

**R5 劣质导入包劫持自动选包**。minHits=1 + 高频词的包可在其他学校文本上得分。
现状已被「导入弹窗显式选包」结构性缓解（自动选包仅是建议值）；v2 保持
「弹窗默认选中预测包 + 允许改选」即可，不引入静默自动链路。

**R6 版本前向不兼容**。旧应用导入 schemaVersion=2 的包会被拒（「不支持的规则包
版本」）。对策仅是文案：报错信息提示「可能需要升级应用到最新版本」。

**R7 单元格内规则组合的非线性交互**。多条规则的匹配顺序产生的组合行为可能
违反作者直觉（前一条吃掉本应后一条处理的行）。这是表驱动 DSL 的固有代价，
对策是命中轨迹诊断（§5 M-A3、§6 M3）+「先到先得」语义在文档中反复强调。

### 3.3 鲁棒性定性结论

防线的层次结构（校验拒绝 → 指纹选择 → 确定性降级 → 人工确认）符合
纵深防御；R1/R2 是仅有的两处「防线缺失」而非「防线可被绕过」，修复成本低、
必须随 v2 一并落地。修复后，残余风险全部属于「错配可被人在环节发现」或
「有明确诊断出口」的类别，鲁棒性达到本应用（本地工具、无服务端）的合理上限。

## 4. 治理规则（随 v2 生效）

1. **解释器冻结原则**：解释器代码此后只允许两类改动——分割/合并机制本身，
   与安全防线。任何新判据（新的行形状、新的括注形态）一律进规则包数据
   （内置包改数据即达），不允许再向解释器添加正则常量。评审新改动时用
   「换个学校它还成立吗」口诀裁决。
2. **隐私红线不变**：包与样本一律占位数据（张三/高等数学A1）；黄金样本库只收
   脱敏样本，入库前逐条检查。
3. **内置包=数据**：内置四包改写为规则数据后，它们与导入包在执行器眼中无差别，
   不再拥有代码特权的「内置路径」。

## 5. rulepack-dev（agent 侧）更新计划

工作流保持「样本 → 读 SKILL → 产包 → 本地验证 → 应用内导入 → 真机复核」，
升级以下组件：

| 阶段 | 内容 | 说明 |
|---|---|---|
| M-A1 | `schema.json` 升 v2 | 新增 courseStart/fieldRules/layout；additionalProperties 收紧；v1 字段标注 deprecated-but-accepted |
| M-A2 | 模拟器 `simulate.py` | Python 镜像规则执行语义（块切分 + 字段抽取），输入包 + OCR 样本，输出每格：字段值 + 命中规则 + 未识别行清单；并入 validate.py 作为 `--simulate` 模式 |
| M-A3 | 黄金样本库 `examples/golden/` | 三布局族各 ≥1 份占位样本 + 期望解析结果；模拟器对其全量自检（可一条命令跑通）；此后每适配一所新学校，追加一份脱敏样本入库 |
| M-A4 | `knowledge.md` / `SKILL.md` v2 | 规则写法教程、机制/判据裁决口诀、常见菜谱（职称括注、【周次】同行、教室行形状、防误判守卫）、不可行边界清单；强调 R7 的先到先得陷阱 |
| M-A5 | 打磨验收 | 模拟器对三份黄金样本输出与 Kotlin 单测期望一致（人工比对一次）；README 更新工作流 |

M-A2/M-A3 是决定包作者实际效率的关键：AI 产包从「写完→导真机→看结果」的
分钟级反馈环，变成「写完→秒级模拟→改」的本地闭环，真机只剩最终确认。

## 6. 应用内更新计划

按 `RULEPACK_V2_PLAN.md` M1–M5 执行，其中并入下列修正与新增项：

| 阶段 | 内容 | 与既有计划的差异 |
|---|---|---|
| M1 | v2 schema + 规则执行器（含命中轨迹输出）+ v1 兼容编译 | 执行器从第一版就返回命中轨迹（原计划放在 M3），避免 M1 接口返工 |
| M2 | 内置四包改写为规则数据；删除解释器内 5 处启发式 | 30 单测全绿为完成标准；`digitAliasOk` 防误判守卫迁移为 zfsoft 包 courseStart 的守卫规则 |
| M3 | 诊断升级：`ocr_debug.txt` 按格输出命中轨迹 + 未识别行；确认界面标注兜底来源字段 | 新增 R3 对策（兜底来源标注） |
| M3.5 ⚠ | **安全加固（新增）**：导入入口大小上限 256KB（R2）；导入期 ReDoS 对抗探测 + 看门狗限时拒收（R1）；R6 报错文案 | 既有计划未覆盖，本评估新增 |
| M4 | rulepack-dev v2 配套（= §5 M-A1..A4） | 不变 |
| M5 | 三样本端到端回归 + 真机验证 | 不变；双线原则：本阶段全部为 master 特性开发，LTS 线不随动，仅当期间发现影响两线的缺陷时按惯例同步修复 |

**M1+M2 完成即达成核心目标**（新格式零发版适配）；M3/M3.5/M4 决定包作者的
适配效率与防线完整性，建议同批做完（合计 4–5 天）。

后续路线（v2 之后，均带触发条件，避免无样本空设计）：

- **P1 应用内「试解析」**：规则包管理页粘贴/选择 OCR 调试文本 → 直接用当前执行器
  跑出逐格结果与轨迹。执行器即解释器，零新增解析逻辑；远程协助链路从
  「用户导出 debug 文件 → AI 盲写包 → 导入试」压缩为用户自查。触发：v2 发布后。
- **P2 包流转约定**：id 命名规范（`zf-校名缩写` / `qz-校名缩写`）、version 递增
  约定、分享文案模板（提醒样本隐私）。触发：出现第二所同校用户。
- **P3 列表式布局（list）**：需新增第三种分割机制。触发：真实用户样本出现。
  字段规则层届时零改动复用。
- **P4 Excel 进规则包管控**：XLS 文本层无 OCR 噪声，差异主要在单元格坐标而非
  文本判据；若做，方向是包内提供列映射提示而非复用正则判据层。触发：XLS 适配
  需求累积（当前样本仅一种格式，缓）。

## 7. 风险回看

| 风险 | 对策 | 状态 |
|---|---|---|
| 执行语义与 v1 不一致破坏三样本 | 30 单测 + 三样本回归为 M1/M2 完成标准 | 计划内 |
| 正则灾难性回溯 | 限长 + 编译校验 + **导入期对抗探测（M3.5，新增）** | 本评估补齐 |
| 模拟器与 Kotlin 语义漂移 | 模拟器只提示不裁决 + 黄金样本库双向对照 | 计划内强化 |
| 启发式再次向解释器堆积 | §4 解释器冻结原则 | 新增，需长期执行 |

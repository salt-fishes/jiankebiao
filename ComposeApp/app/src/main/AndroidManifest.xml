<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 前台解析服务：OCR 解析耗时较长，独立前台运行避免界面被回收 -->
        <service
            android:name=".ScheduleParseService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Room 数据对外共享（供桌面小组件跨进程查询） -->
        <provider
            android:name=".data.ItemContentProvider"
            android:authorities="com.example.composeapp.provider"
            android:exported="true" />

        <!-- 桌面小组件 -->
        <receiver
            android:name=".widget.ItemWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/item_widget_info" />
        </receiver>

    </application>

</manifest>

# 解析规则包架构 v2 计划（规则驱动的可扩展适配层）

> 状态：规划中（v1 已上线并完成三格式端到端验证：正方 PDF / 强智 PDF / 正方网页截图）
> 关联代码：`ComposeApp/app/src/main/java/com/saltfish/simple/schedule/`、`rulepack-dev/`

## 1. 背景与问题

v1 的设计意图是「解释器只有一套，新教务系统只写规则包、不发版」。
但实测适配三个真实样本后暴露了架构漂移：**每适配一个格式，都要往解释器里加启发式**。
仅一轮适配就新增了 5 处解释器级特判：

| 新增启发式 | 服务的格式 | 本质 |
|---|---|---|
| 行尾数字标记防误判（`学分:3.0` 不算课程块） | 正方 PDF | 规则（正则可表达） |
| 「名字+职称括注」教师识别（图标被 OCR 吞掉后） | 正方网页 | 规则 |
| 「名字+【周次】」同行教师识别 | 强智 PDF | 规则 |
| 教室行形状（剥尾部括注、排除节/周字、首个匹配） | 强智/网页 | 规则 |
| 教学班编号行形状 | 网页 | 规则 |

这些全部是正则可表达的「判据」，却被写成了「机制」。照此演化：
- 每所学校 / 每个系统变体 → 改解释器 → 发版；用户手里的老版本永远追不上；
- 启发式互相叠加，行为越来越难推断，回归风险持续累积；
- 规则包退化为「词表 + 少量映射」，表达力远低于各格式差异所需。

## 2. 目标与非目标

**目标**
1. 新教务系统格式的适配 **100% 通过规则包数据完成**，不改解释器、不发版；
2. 规则包保持**纯数据**红线：只有词表、正则、映射与白名单变换，无任何可执行逻辑；
3. v1 包零迁移（旧 JSON 导入即用）；
4. 规则包作者（人或 AI）能**本地闭环调试**，不依赖真机反复试。

**非目标**
- 不引入脚本引擎 / Lua / JS（违反纯数据红线）；
- 不引入联网 LLM 解析（违反不联网红线）；
- 不追求覆盖「任意版式」——明确支持三类布局，其余显式不支持（见 §7）。

## 3. 核心判据：解释器留「机制」，规则包给「判据」

| 归解释器（机制，全行业通用，极少动） | 归规则包（判据，按校配置） |
|---|---|
| band 分区 OCR、IoU 去重、网格列重建、色块切分 | 布局提示：layout 类型、星期数、有无时间轴 |
| 块切分的执行（逐行测试、开新块、续行归并、孤儿行处理） | **块起始规则**：有序正则列表（含课名/类型捕获组声明） |
| 字段抽取的执行（按序试规则、捕获组、白名单变换、覆盖策略） | **字段规则**：有序的 `{正则, 语义字段, 捕获组, 变换}` |
| 周次展开数学、单双周过滤、同名合并、条目重叠合并 | 周次/节次/单双周正则与词表（v1 已有） |
| 安全：正则限长（≤200）、词表上限、语义字段白名单、编译校验 | 噪声词表 labelWords / legendWords（v1 已有） |

判断口诀：**「换个学校它还成立吗？」成立 → 解释器；只对特定系统成立 → 规则包。**

## 4. Schema v2 设计

在 v1 基础上新增两个规则列表（`schemaVersion: 2`，向后兼容字段全部保留）：

```json
{
  "schemaVersion": 2,
  "id": "my-school",
  "name": "某校自定义包",
  "table": {
    "layout": "grid",                // grid | cards | list（list 后置，见 §7）
    "headerAnchor": "时间段",
    "dayPattern": "星期",
    "dayCount": 7
  },
  "courseStart": [
    { "pattern": "(.+?)（?(理论|实践|实验|上机|实训)）?$", "nameGroup": 1, "typeMap": { "理论": "讲课", "实践": "实践", "实验": "实验" } },
    { "pattern": "(.+)[★○●◇:]$", "nameGroup": 1, "type": "讲课" }
  ],
  "fieldRules": [
    { "field": "teacher", "pattern": "[^一-龥]{0,2}([一-龥]{2,4})[（(](高等学校|教师|讲师|教授|助教|无|职称)" },
    { "field": "teacher", "pattern": "([一-龥]{2,4})(?=【)" },
    { "field": "teacher", "pattern": "^([一-龥]{2,6})$", "line": "last" },
    { "field": "room",    "pattern": "([一-龥A-Za-z][一-龥A-Za-z0-9]{1,11})", "stripTrailingParen": true, "splitCampus": true, "excludeChars": "节周" },
    { "field": "classNo", "pattern": "(\\(\\d{4}-\\d{4}-\\d{1,2}\\)-[A-Za-z0-9-]+)" },
    { "field": "credit",  "pattern": "学分[:：]?(\\d+\\.\\d+)" }
  ]
}
```

要点：
- `courseStart` / `fieldRules` 都是**有序列表，先到先得**；解释器按序执行，第一条命中即停；
- 变换函数白名单：`stripTrailingParen`（剥尾部括注）、`splitCampus`（按空格拆校区/教室）、
  `cleanTeacher`（剥职称括注）、`excludeChars`（候选行排除字符）；只能组合、不能自定义；
- 行前缀（linePrefixes）、键值式（keyMap）**保留为语法糖**：编译期展开成等价规则
  （前缀 → `^prefix(.*)$`；keyMap → 边界锚定的键值正则），v1 包零迁移；
- 现有解释器启发式**全部降级为内置包的默认规则数据**（zfsoft / icon-grid / qz / generic 四包改写），
  解释器代码里不再认识「职称括注」「强智」这些概念。

### 安全模型（不变量）
- 单条正则 ≤ 200 字符，列表 ≤ 24 条，词条 ≤ 24 字符（沿用 v1 上限，按新结构套用）；
- 语义字段白名单 + 变换白名单，导入时全量编译校验，失败即拒；
- 规则只在「被匹配文本 ≤ 4000 字符」的单元格上运行，灾难性回溯风险与 v1 相同且受控。

## 5. 解释器改造点

1. `ScheduleParser.parseBlock` 重写为规则执行器：
   - 输入：单元格行列表 + 编译后的 v2 规则集；
   - 输出：`CellCourse` + **命中轨迹**（哪条规则、哪一行、捕获了什么）；
2. `matchBlockStart` 改为执行 `courseStart` 规则列表（v1 typeMarks/markAliases/行尾0兜底
   编译为内置 zfsoft 包的默认 courseStart 规则，防误判守卫随规则迁移）；
3. PDF / 图片两路不变（分割与 v1 一致）；`RulePackStore.pickFor` 指纹选包不变；
4. 诊断升级：`ocr_debug.txt` 按单元格输出「规则命中轨迹 + 未识别行清单」，
   配合 rulepack-dev 工作台定位缺失规则。

## 6. rulepack-dev 工作台配套

1. `schema.json` → v2（含 courseStart / fieldRules / layout，additionalProperties 收紧）；
2. `validate.py` 增加两个能力：
   - **规则模拟器**：用 Python 镜像实现解释器的规则执行语义（块切分 + 字段抽取），
     对着用户样本的 OCR 文本直接试跑，打印每个单元格的规则命中与未填字段；
   - 指纹自检沿用 v1；
3. `knowledge.md` 增补 v2 规则写法与占位示例；`examples/` 增加一个 v2 示例包（占位数据）；
4. 工作流不变：用户给样本 → AI 读 SKILL.md → 产出规则包 → 模拟器本地验证 → 应用内导入 → 真机复核。

## 7. 边界（显式不支持）

- **列表式布局**（一行一课，无网格）：需要第三种分割策略（list layout）。
  字段规则层完全复用，分割层独立。**后置**：等真实用户样本出现再做，避免无样本空设计；
- 课名与字段完全粘连、无任何结构特征的自由文本：规则无法表达，遇到再评估（预期极少）；
- 联网解析 / AI 解析：红线，不做。

## 8. 里程碑与工作量

| 阶段 | 内容 | 预估 |
|---|---|---|
| M1 | v2 schema + 规则执行器 + v1 兼容编译 | 1–1.5 天 |
| M2 | 内置四包改写为规则数据 + 单测迁移（现有 30 个用例全部保留） | 0.5 天 |
| M3 | 结构化诊断（规则命中轨迹） | 0.5 天 |
| M4 | rulepack-dev v2（schema / 模拟器 / 示例 / 文档） | 1 天 |
| M5 | 三样本回归（正方 PDF / 强智 PDF / 网页截图端到端）+ 真机验证 | 0.5 天 |

合计约 3.5–4 天。M1–M2 完成即达到「新格式零发版适配」；M3–M4 决定包作者的实际适配效率。

## 9. 风险与对策

- **风险**：规则执行语义与 v1 行为不完全一致，破坏已验证的三个样本。
  对策：现有 30 个单测 + 三样本端到端作为回归基线，M1 完成标准 = 全绿；
- **风险**：正则规则组合出灾难性回溯。对策：沿用被匹配文本限长 + 导入期编译校验 + 限长上限；
- **风险**：Python 模拟器与 Kotlin 解释器语义漂移。
  对策：模拟器只做「试跑提示」，真值以应用内导入结果为准；文档标注此边界。
